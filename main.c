#include "push_swap.h"

int main(int argc, char **argv)
{
	int	strategy;

	strategy = 0;
	// Controlla: argc > 1. 
	// Caso 1> ./push_swap "numeri"
	// Caso 2> ./push_swap n0 n1 n2 n3 ...
	// Caso 1bis, 2bis> ./push_swap --strategy_selector (adaptive(caso base), simple, medium, complex)
	if ()
	{

	}
	// Parsing chiamo
	
	// Qui abbiamo creato la lista. Dobbiamo chiamare una funzione che assegni gli indici.
	(void) assign_indexes(t_stack *a)
	
	// Calcoliamo il disordine compute_disorder=  mistakes / total_pairs
	
	// Funzione che se stragy e' 0 cambia strategy: 1(simple) < 20 percento, 2(medium) tra >= 20 e <= 65, 3(complex) sopra > 65
		
	// Stampiamo i passi dell'algoritmo scelto 
		
}
